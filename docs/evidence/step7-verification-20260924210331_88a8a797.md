# Step 7 attempt 20260924210331_88a8a797: verification note

This is the only measured Step 7 attempt for #141, and the exported evidence in
`step7-20260924210331_88a8a797.*.json` comes from it.

- **Run:** 2026-09-24, 21:07–21:17 UTC.
- **Code under test:** the hub, the agents, the driver and the preflight ran at
  coordination commit `cced6cc87ef17607beebb511cbe6a8fa9d02fe8d`, which the
  manifest records as `coordination_head`.
- **First verify, at `cced6cc`:** failed, with 70 of 73 checks. All 12 `step7_*`
  checks passed. The three failures were `review_comment_<task>`, one for each of
  Charlie's review comments on sandbox PR #16.
  - Alice's skill template is "Reviewer agent `<name>` on behalf of `<account>`".
    She filled `<name>` with the hub agent name, so every reviewer assignment said
    "Reviewer agent charlie on behalf of RoboNater".
  - Charlie wrote exactly that. Each comment otherwise carried the reviewed SHA,
    the verdict and the test command.
  - The verifier required the capitalized "Charlie".
- **Fix:** #145, merged as `5cbc989d9e4170b376c682b7230344d6a1b94d90`, made the
  verifier accept any case of the agent name and changed nothing else. The role
  wording, the name itself and the account stay exact.
- **Re-verify, with #145:** the same run directory, unchanged, passed 73 of 73.
  `verify` re-reads the durable hub, GitHub and transcript facts, so nothing from
  the run was edited or re-executed.
- **First verifier output:** committed unchanged as
  `step7-verification-20260924210331_88a8a797.first-verify.json` (sha256
  `1ab8f6b4fc5766b7a012e5a95349af7d0f2f0be673fc1ef18590bb3b52455400`). It holds
  only check names and results. Compared with
  `step7-20260924210331_88a8a797.evidence.json`, only these differ:
  - the three `review_comment_<task>` checks;
  - `passed`;
  - `failed_checks`.

  The two runs of `verify` wrote byte-identical `github-facts.json`,
  `hub-audit.json` and `tool-audit.json`. The same facts were judged twice.

The operator chose to re-verify instead of running a fresh attempt. The runbook
says a verifier-requirement change "normally requires a fresh live proof". Here
the change loosens a single string comparison that the run's own behaviour did
not violate.

`status: "prepared"` in the exported manifest is expected. `prepare` sets that
field and nothing later updates it, and the Step 6 manifest shows the same value.
The run's outcome is `evidence.json`'s `passed`.
