# Step 6 failed setup: 20260918185141_14adf840

Coordination preparation head: `9c74cd3`. Persistent raw run:
`/home/alfred/lw/step6-runs/issue28-attempt2`. The three full clones, private
token/configs, manifest, and Alice terminal log are preserved.

Sandbox [issue #6](https://github.com/RoboNater/robo-agents-sandbox/issues/6)
was created after 558 passing tests and independent approval to start the run.
Claude Code had updated to 2.1.277; its actual version was recorded, alongside
Codex CLI 0.154.0 and the pinned models.

Alice's launcher exported `CLAUDE_CONFIG_DIR=~/.claude` even when the operator
had not set it. Claude treats this explicit variable differently from its
implicit default: its main configuration lookup changed from `~/.claude.json`
to `~/.claude/.claude.json`, causing first-run onboarding before the hub or
workers started. The control session did not restore a backup or complete
onboarding in that altered configuration root. It stopped this exact Alice
process and marked the manifest failed. No work PR, disturbance, review, or
merge occurred, and the issue remains open.

The launcher now records whether a custom directory was explicitly supplied.
It preserves that custom setting, while leaving the variable unset for the
implicit default. Fake-runtime regressions inspect the actual child environment
for both cases. A fresh namespace is required after this launcher change.
This failed setup does not complete Step 6 or close #28/#29.
