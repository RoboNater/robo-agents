# Step 6 failed topology proof: 20260918185642_a915f32f

Coordination preparation head: `d3d73f5`. Persistent raw run:
`/home/alfred/lw/step6-runs/issue28-attempt3`. All three clones, private configs,
SQLite state, and runtime transcripts are retained.

Sandbox [issue #7](https://github.com/RoboNater/robo-agents-sandbox/issues/7) and
[work PR #8](https://github.com/RoboNater/robo-agents-sandbox/pull/8) remain open.
Alice/Bob ran Claude Code 2.1.277 / `claude-sonnet-5`; Charlie ran Codex CLI
0.154.0 / `gpt-5.6-sol`, with correctly reported distinct persisted clone IDs.

Bob completed the draft at `32d3d32db183ed9ffa4d49391100402903e73fcf`.
Charlie posted `changes_requested` with both `r1-1` and `STEP6-NORMALIZE-001`.
Bob resolved it at `dbcefd582337c36e068a43b0e6a285d3151396da`, Charlie approved,
and the driver pushed its declared head canary at
`b7a95db46c83efcc3cd30d87c265e70cd3ac6f92`. No base PR or work merge occurred.

However, Charlie treated his clone's `.git` as read-only under Codex's default
workspace-write protection. He fetched GitHub API archives and tested temporary
snapshots instead of checking out the assigned heads in the persisted clone.
Those reviews were independent of Bob's working tree, but they do not prove the
plan's required full-clone review choreography. The control session stopped all
identified run children and the hub listener and marked the manifest failed.
It did not push fixes for Bob, review for Charlie, merge for Alice, or reset work.

A separate disposable Codex preflight with `--add-dir <own-clone>/.git`
successfully fetched and checked out a local origin while retaining the
workspace-write sandbox and automatic approval review. The next launcher grants
only that Git metadata directory. A trusted review-check helper now checks the
actual HEAD and identity, runs tests in the persisted clone, and records private
audit facts correlated to the executed CLI output and each review task. Archive
snapshots, absent audits, and wrong clone/head/identity/timing fail verification.
A fresh measured namespace is required after these launch/verifier changes.
This attempt does not complete Step 6 or close #28/#29.
