# Step 6 failed attempt: 20260918135514_e70dd993

Coordination preparation head: `e2ea547`. Persistent raw run:
`/home/alfred/lw/step6-runs/issue28-attempt1`. The run is preserved intact with
its token/configs excluded from Git, all clones, SQLite state, and CLI logs.

Sandbox [issue #4](https://github.com/RoboNater/robo-agents-sandbox/issues/4) and
[work PR #5](https://github.com/RoboNater/robo-agents-sandbox/pull/5) remain open.
Alice and Bob ran Claude Code 2.1.276 / `claude-sonnet-5`; Charlie ran Codex CLI
0.154.0 / `gpt-5.6-sol`, with distinct persisted clone IDs.

The real workflow completed IMPLEMENT, a blocking review (`r1-1`, missing
casefold with a failing mixed-case assertion), ADDRESS, approval, the declared
post-approval head push, an actual `head_matches=false` gate, and RE-REVIEW.
Alice refused to merge the old approval and Charlie approved the moved head.

Relevant heads:

- Draft: `97e18bc3ab6af9e39d609bd4ef37767b2b93c922`.
- Addressed/approved: `0c62a9b62f8e2c1554b5dbdfa912176b807242b3`.
- Driver-moved/re-reviewed: `a8b66a110330c410780f014f340409fd9c29acef`.
- Unpublished base fixture commit: `7eb626e644837e225b81d749889371739d4950a4`.

The new base-branch push failed because its detached-HEAD destination was not
qualified with `refs/heads/`. No base PR or base merge occurred. The corrected
driver has a disposable real-Git regression covering both detached-HEAD pushes,
including reproduction of the original short-ref rejection. Charlie also
omitted the fixture's required `STEP6-NORMALIZE-001` tag, so this attempt cannot
satisfy the strict verifier even though the substantive finding was correct.
The next trusted prompt transmits that requirement explicitly in the REVIEW
assignment. A separate independent code review found further evidence and
recovery gaps; those were fixed before a fresh measured namespace.

The control session marked the manifest failed and stopped only this run's
identified hub, Alice, Bob, Charlie, and MCP children. The listener is stopped.
It did not push fixes for Bob, review for Charlie, merge for Alice, close the
issue/PR, delete branches, or reset/delete any clone. This attempt is not
acceptance evidence and does not complete Step 6 or close #28/#29.
