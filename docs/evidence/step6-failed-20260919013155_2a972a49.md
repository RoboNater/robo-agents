# Step 6 native-Windows mixed-harness attempt: 20260919013155_2a972a49

Coordination head at preparation: `1314e02` (branch `step6-windows-mixed-harness`).
Persistent raw run: `C:\n\w\step6-runs\win-attempt1`. Clones, SQLite state,
private configs, transcripts, telemetry and the verifier's private outputs are
preserved there and not committed.

Topology, all native Windows 11 processes (no WSL):

| Role | Harness | Model |
|---|---|---|
| Alice | Codex CLI 0.154.0 via `codex app-server` supervisor | `gpt-5.6-luna` |
| Bob | Claude Code 2.1.277 via stream-json supervisor | `claude-haiku-4-5-20251001` |
| Charlie | OpenCode 1.18.31 via `opencode serve` + `run --attach` | `openrouter/nvidia/nemotron-3-ultra-550b-a55b:free` |

Sandbox [issue #12](https://github.com/RoboNater/robo-agents-sandbox/issues/12)
is closed; [work PR #13](https://github.com/RoboNater/robo-agents-sandbox/pull/13)
and [base PR #14](https://github.com/RoboNater/robo-agents-sandbox/pull/14) are merged.
The roadmap reservation comment was skipped (`STEP6_SKIP_ROADMAP_RESERVATION=1`).

| Event | SHA |
|---|---|
| Draft; Charlie `changes_requested` with `r1-1` / `STEP6-NORMALIZE-001` | `3f612f3352893edaff4a5457923df4b68009d2a4` |
| ADDRESS; exact-head approval | `a154895d01316d8225a2c366bac0eb709070d8d6` |
| Driver head push; `head_matches=false` gate; RE-REVIEW approval | `e9f444a68526e99bed0fba387b5e71c184da4b86` |
| Base PR #14 merged, main `03271ef` → `eea9e88`; stale-base gate; REBASE (`conflict_files=[]`) | `9f4329742bd7dd5037e8cf686ab5fe565dd5d94a` |
| Alice's `gh pr merge … --squash --delete-branch --match-head-commit 9f43297…` | merge `bf10dca4adf3f39fd27e77f1baed659cfe1f380e` |

Workflow `done`; both workers released after CLOSE-OUT and observed release.
No supervisor sent a continuation (Alice, Bob and Charlie: 0 reprompts).

## Verifier result: failed

The strict verifier passed 54 of 61 checks. The 7 failures are
`head_gate`, `head_gate_response_correlated`, `base_gate`,
`base_gate_response_correlated`, `final_gate`, `final_gate_response_correlated`
and `follow_ups_verified`. They share one cause: Alice logged each `step6:gate:*`
and `step6:follow-ups` rationale as prose followed by JSON
(`Actual check_merge_gate JSON: {...}`) instead of bare JSON as the trusted
prompt requested.

As a diagnostic only, re-evaluating the same run with the single embedded JSON
object extracted passes all 61 checks, including exact correlation of each gate
with Alice's actual `check_merge_gate` tool results. That diagnostic is not
acceptance evidence; the verifier was not relaxed. Issue #71 tracks making the
evidence independent of rationale formatting.

## Deviations and observations

- Alice ran unattended through a Codex app-server supervisor rather than an
  interactive PTY, so one process keeps the hub MCP child alive across turns.
- OpenCode has no OS sandbox. Charlie ran with edits and external directories
  denied and a narrow shell allowlist. Two `gh pr comment` attempts were denied
  because backticks in the body became command substitutions outside the
  allowlist; Charlie retried without them. OpenCode's `glob` tool failed on
  Windows (`ripgrep execution failed`).
- The run-local Codex home hard-links `~/.codex/auth.json` because the account
  cannot create symlinks; skills are copied for the same reason.
- `python3` resolves through pyenv-win with `.python-version` (3.12.10) at the
  run root (`STEP6_PYENV_VERSION`).
- The private sandbox still cannot enable branch protection.

This attempt does not change Step 6's recorded completion.
